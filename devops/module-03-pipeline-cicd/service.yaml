apiVersion: v1
kind: Service
metadata:
  name: todo-api
spec:
  selector:
    app: todo-api        # cible les pods du Deployment
  ports:
    - port: 80           # port du service
      targetPort: 5000   # port de l'application
